.headline-component {
  background: #43436B;
  color: rgba(255,255,255, .5);
}

.headline-container {
  display: flex;
  padding: 15px 0;
  align-items: center;
}

.title {
  color: rgba(255,255,255, .5);
  font-size: 14px;
  margin-left: 25px;
}

.buttons {
  margin-left: auto;
}

.btn {
  color: rgba(255,255,255, .9);
  text-decoration: underline;
  background: transparent;
}