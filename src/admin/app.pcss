.app-container {
  display: flex;
  flex-direction: column;
  min-height: 100vh;
}

.notification {
  transition: transform .3s;
}

.notify-container {
  position: fixed;
  top: 100%;
  left: 50%;
  transform: translateX(-50%);

  &.active {
    .notification {
      transform: translateY(-100%);
    }
  }
}


